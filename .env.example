# Local development falls back to http://localhost:5173 when this is omitted.
# Current GitHub Pages deployment. Keep this active until the customer confirms
# a custom production domain; an approved customerContent.productionDomain value
# will then take precedence for runtime and generated metadata.
VITE_SITE_URL=https://ristohajdukovic.github.io/minoconsult
